import React from 'react';
import IconContainer from '../iconContainer';

import Header from '../header';

const Home = () => (
  <div>
    <Header />
    <IconContainer />
  </div>
);

export default Home;
